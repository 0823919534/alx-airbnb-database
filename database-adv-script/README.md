# database-adv-script

This folder contains the SQL assignment file for '0. Write Complex Queries with Joins'.

Files:
- joins_queries.sql — includes INNER JOIN, LEFT JOIN, FULL OUTER JOIN with UNION fallback.
